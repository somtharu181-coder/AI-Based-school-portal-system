# Core Django
Django==6.0.4
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# Environment & secrets
python-dotenv==1.2.2

# Django REST Framework
djangorestframework==3.17.1

# Machine Learning (analytics engine)
numpy==2.2.6
scikit-learn==1.6.1

# PDF generation (reporting_system)
reportlab==4.4.1

# Excel file processing (bulk upload)
openpyxl==3.1.5

# Image processing (CAPTCHA generation)
Pillow==11.2.1

# Rate limiting (login brute-force protection)
django-ratelimit==4.1.0

# Production WSGI server
gunicorn==23.0.0

# Production database (PostgreSQL — optional, for migration from SQLite)
# psycopg2-binary==2.9.10

# Static file serving in production
whitenoise==6.9.0

# Security: JWT (for API token auth)
djangorestframework-simplejwt==5.5.0
